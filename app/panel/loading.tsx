/**
 * Fallback de carga del panel del artista.
 *
 * Estaba en `app/loading.tsx` (raíz), donde causaba soft 404: ver la nota en
 * `components/layout/CargandoPanel.tsx`.
 */
export { default } from "@/components/layout/CargandoPanel";
